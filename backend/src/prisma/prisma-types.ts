// This file ensures Prisma types are available in the workspace
// It can be deleted if not needed
import type { categories, products, CategoryStatus, ProductStatus } from '@prisma/client';

export type { categories, products, CategoryStatus, ProductStatus };
