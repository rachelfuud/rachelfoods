# Railway Backend Service Configuration
[build]
builder = "NIXPACKS"
buildCommand = "cd backend && npm ci && npm run build && npx prisma generate"

[deploy]
startCommand = "cd backend && npx prisma migrate deploy && node dist/src/main.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
